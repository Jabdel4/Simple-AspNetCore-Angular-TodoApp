name: build-api

trigger:
    batch: true
    branches:
        include:
        - feature/todo-api
    paths:
        include:
        - pipelines/builds/build-api.yml
        - TodoApi/*
        exclude:
        - /*.md
        - /*.gitignore

variables:
- template: ../variables.yml

jobs:
- template: ../templates/build-template.yml